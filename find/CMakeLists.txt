
CMAKE_MINIMUM_REQUIRED(VERSION 3.5)
SET(CMAKE_CXX_STANDARD 11)

PROJECT(LibraryFinder VERSION "0.1")


#Find JPEG
FIND_PACKAGE(JPEG)
IF (JPEG_FOUND)
    MESSAGE("++ JPEG Found")
ENDIF (JPEG_FOUND)


#Find SDL
FIND_PACKAGE(SDL)
IF (SDL_FOUND)
    MESSAGE("++ SDL Found")
ENDIF (SDL_FOUND)


